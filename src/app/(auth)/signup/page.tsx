import SignUp from "@/components/Auth/SignUp/SignUp";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SignUp />
    </div>
  );
};

export default page;
